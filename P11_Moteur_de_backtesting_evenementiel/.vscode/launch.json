{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run tier2_backtester",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/target/debug/tier2_backtester.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "cargo build"
        }
    ]
}